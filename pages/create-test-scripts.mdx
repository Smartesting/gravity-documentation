import Layout from '../components/layout'

export const meta = {
  headerTitle: 'Creating test scripts',
}

## Get a boostrap project
## Export only one test

export default ({ children }) => <Layout meta={meta}>{children}</Layout>